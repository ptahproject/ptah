Memphis config
==============

This package provides:

- two stage registration subsystem for zope component registry.

- global application settings subsystem allow to load/save settings 
  from file, alow it allows to introspect all registerd settings in application
