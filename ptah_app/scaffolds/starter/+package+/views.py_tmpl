from memphis import view
from {{package}}.app import ApplicationRoot

view.static('{{project}}', '{{project}}:static')

@view.pyramidview('view', context = ApplicationRoot)
def my_view(request):
    return "A view"
